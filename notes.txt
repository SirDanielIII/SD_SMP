When updating a version, if an option is missing the entire thing gets dumped onto preexisting values
When reloading the document, check if things are missing or not and add back the default values


Changelog
- Reworked health scoreboard to be compatible with other scoreboards from other plugins/minigames/pre-existing scoreboards

Next thing to do:
    Test out Coord.showAllCoords
    Double check if the main world is the first one in the Bukkit.getWorlds() world list
    Check if Bukkit.getWorld([name]) is always lowercase or not (the return, I mean)
    Test out clicking on a Coord to teleport

/coords send here SirDaniel_da3rd saodjsad adl dasoi dsaji dio sajd jij dsaj ijds ij dj i jd
-> Add limit to how many people are listed, or put it into one message

Add "/coords set " + args[1] + " here --force" to documentation
-> Auto detect duplicates

Add option to detect world for
/coords send yeet <players>

Add global top scoreboard

Future Features
1. Create a teleport history for player so they can do /coords back to go back
2. Cache the results of default worlds in CoordsUtility
3. Store a server id under each world
4. /coords track [coordinate]
5. /coords pulls up a GUI instead, with the original /coords replaced with /coords help
6. Put the coord names in alphabetical order so we can utilize binary search (add toggle in config)